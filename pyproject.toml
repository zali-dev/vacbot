[project]
name = "vacbot"
version = "0.1.0"
requires-python = ">=3.12"
dynamic = ["dependencies"]

[tool.vercel]
entrypoint = "run_web:app"

[tool.uv]
requirements-txt = ["requirements.txt"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
